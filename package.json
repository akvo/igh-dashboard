{
  "name": "igh-dashboard",
  "private": true,
  "scripts": {
    "check:all": "cd frontend && npm run content:check && npm run build && npm test && cd ../backend && npm run test:all && npm run check"
  }
}
